local function orbs()
local lib = require(game:GetService("Workspace").upsilonLibrary)
for i,v in pairs(workspace:GetDescendants()) do
      if v:IsA("BasePart") then 
lib.FireOtherClients("openVent",v)
      end
   end
end

for i = 1,500 do

coroutine.wrap(orbs)()

end
